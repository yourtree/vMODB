<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dk.ku.di.dms.vms</groupId>
    <artifactId>vms-runtime-java</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>sdk-embed</artifactId>
  <version>1.0-SNAPSHOT</version>
  <name>sdk-embed</name>

  <dependencies>

    <!-- modb -->
    <dependency>
      <groupId>dk.ku.di.dms.vms</groupId>
      <artifactId>modb</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>

    <!-- sdk-core -->
    <dependency>
        <groupId>dk.ku.di.dms.vms</groupId>
        <artifactId>sdk-core</artifactId>
        <version>1.0-SNAPSHOT</version>
    </dependency>

    <dependency>
      <groupId>dk.ku.di.dms.vms</groupId>
      <artifactId>modb-common</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>

    <dependency>
      <groupId>dk.ku.di.dms.vms</groupId>
      <artifactId>web_common</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <!-- Enable Java 21 preview features at compile time -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>21</source>
          <target>21</target>
          <compilerArgs>
            <arg>--enable-preview</arg>
            <arg>--add-exports</arg>
            <arg>java.base/jdk.internal.misc=ALL-UNNAMED</arg>
            <arg>--add-opens</arg>
            <arg>java.base/jdk.internal.misc=ALL-UNNAMED</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- Force Surefire to run tests, pass preview flags, and ignore failures -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <!-- Ensure tests are not skipped -->
          <skipTests>false</skipTests>
          <skip>false</skip>
          <!-- Pass JVM preview flags to test JVM -->
          <argLine>
            -Djava.library.path=${project.basedir}/../web_common/src/main/java/dk/ku/di/dms/vms/web_common/iouring/lib
            --enable-preview
            --add-exports java.base/jdk.internal.misc=ALL-UNNAMED
            --add-opens java.base/jdk.internal.misc=ALL-UNNAMED
          </argLine>
          <!-- If no tests are found, do not fail the build -->
          <failIfNoTests>false</failIfNoTests>
          <!-- Continue executing remaining tests even if some fail -->
          <testFailureIgnore>true</testFailureIgnore>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

